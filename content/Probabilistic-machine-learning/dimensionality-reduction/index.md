(sec:DimensionalityReduction)=
# Dimensionality reduction

In this chapter we consider some aspects of dimensionality reduction of data.
In particular we consider SVD, which is "singular value decomposition", and its role in PCA, which is "principle component analysis".

